<!DOCTYPE html>
<html lang="en">
<head>
  <title>tiny app</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body style="font-size:20px; padding:50px;">
  <%- include('partials/_header') %>
  <h4>urls generated</h4>
  <ul>
    <% for (let shortURL in urls) { %>
      <li><%= shortURL %> &rarr; <a href= <%= urls[shortURL]%>><%= urls[shortURL] %></a>
        <form method="POST" action= <%= `/urls/${shortURL}/delete` %>>
          <button type="delete" value="delete" class="btn btn-danger">delete</button>
          <a class="btn btn-primary" href=<%= `/urls/${shortURL}/` %> role="button">edit</a>
        </form>
      </li>
    <% } %>
  </ul>
</body>
</html>


