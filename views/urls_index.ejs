<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <%- include('partials/_header') %>
  <h2>url list</h2>
  <ul>
    <% for (let shortURL in urls) { %>
      <li><%= urls[shortURL] %> - <a href=<%= urls[shortURL]%>><%= shortURL %></a></li>
    <% } %>
  </ul>
</body>
</html>

